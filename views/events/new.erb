<h2>Add Event</h2>

<form action="/events" method="post">

  <label for="name">Event Name:</label>
  <input type="text" name="name"><br>

  <label for="nation">Select an Event:</label></font>
      <select name="event_id">
       
        <% @events.each do |event|  %>
          <option value=<%=event.id %> > <%= event.name %> </option>
        <% end %>
      </select>

      <label for="event">Select an Athlete:</label></font>
          <select name="event_id">
           
            <% @events.each do |event|  %>
              <option value=<%=event.id %> > <%= event.name %> </option>
            <% end %>
          </select>

  <input type='submit' value="ADD AN ATHLETE">
</form>

<br>
<br>
<a href="/">Back</a>